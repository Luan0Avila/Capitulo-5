users = ['luan', 'lucas', 'joão', 'admin', 'mateus']

if users:
    for user in users:
        print (user)
    print ('A lista não está vazia!')

users.clear()

if user:
     for user in users:
        print(user)
     print ('A lista está vazia!')

